#include <stdio.h>

/**
 * main - Entry point
 * Return (0): (Success)
 *
 */

int n=0
{
if (n>0)
	printf("n is positive")
else if (n=0)
	printf("n is zero")
else if (n<0)
	printf("n is negative")
	return (0);
}
